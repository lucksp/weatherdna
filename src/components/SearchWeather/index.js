import React from "react";
import PropTypes from "prop-types";

const SearchWeather = props => {
  return <div>Search for Weather</div>;
};

SearchWeather.propTypes = {};

export default SearchWeather;
